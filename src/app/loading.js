"use client"
import LoadingPage from '@/shared/Loading';
import React from 'react';

const loading = () => {
    return (
        <div>
            <LoadingPage/>
        </div>
    );
};

export default loading;